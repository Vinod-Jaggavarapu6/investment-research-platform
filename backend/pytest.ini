[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: requires a running DB and Redis (opt-in with -m integration)
    live: requires real LLM APIs and external network (opt-in with -m live)
addopts = -m "not integration and not live"
